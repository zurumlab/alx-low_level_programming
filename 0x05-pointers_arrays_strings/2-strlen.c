#include "main.h"
#include <string.h>

/**
 * _strlen - function to get the lenght of a string
 * @s: pointer parameter
 * Return: length of the string
 */

int _strlen(char *s)
{

return (strlen(s));
}

