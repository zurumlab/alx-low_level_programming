#include <stdio.h>


/**
 * main - a function with arguments
 * @argc: counts the number of arguments in the function
 * @argv: array of variables passed toba function
 * Return: 0 if successful
 */

int main(int argc, char *argv[] __attribute__((unused)))
{
printf("%d\n", argc - 1);
return (0);
}
