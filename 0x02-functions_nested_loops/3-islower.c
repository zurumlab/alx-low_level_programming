#include "main.h"

/**
 * _islower - checks for numbers that are lower cases
 *
 * @c: a c program
 *
 * Return: 0
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
	else
	return (0);
}
