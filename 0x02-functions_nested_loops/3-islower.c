#include "main.h"

/**
 * islower - checks for numbers that are lower cases
 *
 * @c: a c program
 *
 * Return: 0
 */
int islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
	else
	return (0);
}
